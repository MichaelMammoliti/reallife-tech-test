import React from 'react';

import { DefaultPageWrapper, Container } from '@components';
import { MoviesContainer } from '@containers';

const Home = () => (
  <DefaultPageWrapper>
    <MoviesContainer />
  </DefaultPageWrapper>
);

export default Home;
